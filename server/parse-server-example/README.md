## RED APPLE SERVER DEPLOY

To launch the dashboard: 

`parse-dashboard --appId myAppId --masterKey masterKey --serverURL "http://localhost:1337/parse" --appName "Red Apple Server"`

To launch the server:

`npm install`
`npm start`


Testing here